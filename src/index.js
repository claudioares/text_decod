import TextProcess from './scriptClass';
const textprocess = new TextProcess();

const buttonCripty = document.querySelector('.cripty');

buttonCripty.addEventListener('click', ()=>{
  alert("buttom cripty")
  // textprocess.handleCripty();
})